name: mamai
description: mamAI - Memory Agenda Master AI

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  vosk_flutter: ^0.2.1
  audioplayers: ^5.2.1
  http: ^1.2.0
  permission_handler: ^11.3.0
  shared_preferences: ^2.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/models/vosk-model-small-fr/
